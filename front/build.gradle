plugins {
    id 'com.moowork.node' version '1.3.1'
}

node {
    version = '18.0.0'
    npmVersion = '8.1.0'
    download = true
}

task install(type: NpmTask) {
    args = ['install']
}

task build(type: NpmTask, dependsOn: install) {
    args = ['run', 'build']
}